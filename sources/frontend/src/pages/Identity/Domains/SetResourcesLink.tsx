import React from "react";
import { Domain } from "src/models/Domain";

interface Props {
  domain: Domain;
}

export const SetResourcesLink: React.FC<Props> = (props) => {
  return <>link</>;
}
